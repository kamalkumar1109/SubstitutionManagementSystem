/**
 * Demo seed schools (GREENFIELD / OAKRIDGE) are no longer created.
 * Platform owners add real schools from Admin → Schools.
 */
console.log("[seed] Demo school seeding is disabled. Create schools from Admin → Schools.");
console.log("[seed] To remove leftover GREENFIELD/OAKRIDGE records: npm run purge:demo --workspace server");
process.exit(0);
